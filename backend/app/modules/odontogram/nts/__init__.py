"""NTS (Norma Técnica de Salud) support for the odontogram module.

Scope of this package today (NTS-03): the *normative catalog* only — a
versioned, validated, read-only description of what a norm requires. No
clinical persistence, no findings, no renderer, no endpoints.
"""
